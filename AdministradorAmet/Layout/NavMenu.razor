@inject NavigationManager NavigationManager
<PageTitle>Administrador Digesett</PageTitle>




<div class="top-row ps-3 navbar navbar-dark">
    <div class="container-fluid top-row" href="Home">
        <!-- Logo centrado y un poco más grande -->
        <a href="Home">
            <!-- Ruta a la página de inicio -->
            <img src="imagenes/ametlogo.png" alt="Logo DIGESETT" class="logoarriba" />
        </a>
    </div>
</div>

<div class="@NavMenuCssClass nav-scrollable" @onclick="ToggleNavMenu">
    <nav class="flex-column">
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Home" Match="NavLinkMatch.All" @onclick="() => CloseSubMenu()">
                <img src="imagenes/home-2.svg" alt="Logo DIGESETT" class="nav-icon" />
                <span class="bi bi-house-door-fill-nav-menu" aria-hidden="true"></span> Home
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="UsuariosRadzen" @onclick="() => CloseSubMenu()">
                <img src="imagenes/userlist.png" alt="Logo DIGESETT" class="nav-icon" />
                <span class="bi bi-plus-square-fill-nav-menu" aria-hidden="true"></span> Usuarios
            </NavLink>
        </div>

       @*  <div class="nav-item px-3">
            <NavLink class="nav-link" @onclick="() => ToggleSubMenu()">
                <img src="imagenes/search-normal-1.svg" alt="Logo DIGESETT" class="nav-icon" />
                <span class="bi bi-search" aria-hidden="true"></span> Buscar
            </NavLink>

            @if (isSubMenuVisible)
            {
                <div class="sub-menu">
                    <div class="sub-menu-item">
                        <span class="sub-menu-dash">-</span>
                        <NavLink class="sub-menu-link" href="Buscar">
                            Multa
                        </NavLink>
                    </div>
                    <div class="sub-menu-item">
                        <span class="sub-menu-dash">-</span>
                        <NavLink class="sub-menu-link" href="BuscarAgentes">
                            Agente
                        </NavLink>
                    </div>
                </div>
            }
        </div> *@
        <div class="nav-item px-3">
            <NavLink class="nav-link" href="Buscar" @onclick="() => CloseSubMenu()">
                <img src="imagenes/search-normal-1.svg" alt="Logo DIGESETT" class="nav-icon" />
                <span class="bi bi-plus-square-fill-nav-menu" aria-hidden="true"></span> Buscar
            </NavLink>
        </div>

        <div class="nav-item px-3">
            <NavLink class="nav-link" href="notificaciones" @onclick="() => CloseSubMenu()">
                <img src="imagenes/notification.svg" alt="Logo DIGESETT" class="nav-icon" />
                <span class="bi bi-list-nested-nav-menu" aria-hidden="true"></span> Notificaciones
            </NavLink>
        </div>
    </nav>
</div>

@code {
    private bool collapseNavMenu = true;
    private bool isSubMenuVisible = false;

    private string? NavMenuCssClass => collapseNavMenu ? "collapse" : null;

    private void ToggleNavMenu()
    {
        collapseNavMenu = !collapseNavMenu;
    }

    private void ToggleSubMenu()
    {
        isSubMenuVisible = !isSubMenuVisible;
    }

    private void CloseSubMenu()
    {
        isSubMenuVisible = false;
    }
}
